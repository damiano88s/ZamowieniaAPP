<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen -->
    <style name="Theme.ZamowieniaAPP.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.ZamowieniaAPP</item>
    </style>

    <!-- Normalny motyw -->
    <style name="Theme.ZamowieniaAPP" parent="android:Theme.Material.Light.NoActionBar" />

</resources>